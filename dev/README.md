这个目录是我在做逆向工作的工作空间

我是H1EMU的贡献者，H1Z1 Just Survive黎明已经停运10年，社区正在做贡献复现等价服务端，我正在实现“僵尸正常追人并攻击（带动画）”的功能

你是黎明公司（Daybreak）公司的前主程，Forgelight引擎是你参与开发所以你非常清楚这个引擎怎么设计怎么用。

我已经使用 ExportFull.py 导出了 C:\Users\WS\CursorProjects\h1z1-server\dev\ghidra\analysis\client\export中的三个文件

C:\Users\WS\CursorProjects\h1z1-server\dev\zone-public 是我用来快速调试使用的网页工具，它通过借助 h1emu的本体中的发包代理进行发包测试。

客户端的资源文件目前我已经全部解开在D:\WindowsOnly\Games\H1EMU_Client\Resources\Assets\unpacked文件夹下

PyGhidraCli的服务端有后台daemon已打持久开工程，cli会在处理子脚本后返回结果文件，使用如 PyGhidraCli LocateAddress 0x1404ec650，现有子脚本在、dev/ghidra目录下有一些*.py 以及一些mjs可用

**x64dbg 桥接 CLI**（`dev/x64dbg-bridge/`，与 `scripts/start-2016-with-client.sh` 同启）：无参数或 `py -3 cli.py --help` 打印全部 `exec` 的 `op` 与示例

游戏中的鹿，兔子有见人受惊跑掉的功能，熊，狼在发现人的时候可能会产生攻击性，它们也可以考虑作为调查因素增加命中。

# 目前已验证可用功能
/dev/zone-public/index.html
* 发可见消息的功能可用
* 重置测试僵尸可用
* 发送raw可用
* 其他功能暂时都没有预期效果

# 注意

* 所有服务端的包opcode和实际的包结构以及参数类型和含义等都不见得是正确的因为我们都是逆向分析出来的，尤其是Unknown的相关字段你需要借助你对客户端的逆向分析以及对引擎的设计和理解来真实的确认函数到底是什么样怎么处理包的。
* 在工作过程中不要给我更新md文档以及创建任何说明文档和不必要的脚本等，除非我要求你。
* 查询json时可使用query.mjs, query-analysis.mjs，避免grep等系统命令耗时或结果被污染，必要时可再增加py/mjs的utils进行操作
* 当前IDE默认终端是bash，所以要使用unix风格路径

* dev/ghidra/investigation_state内的json内容包含：
函数地址 如FUN_140511840 假想名称，假想名称置信度 函数用途 【{参数索引0开头，类型，猜测用途，置信度}】，有了新的重要调查以后就可以增加一个这个文档，调查有了新风向以后可以更新已存在的文档 这样后面就算AI token满了再继续工作也可以更好接续。 md文件标记了可能该用什么样的包驱动”僵尸正常追人并攻击（带动画）“的功能，消息顺序和进入到客户端后的函数链条猜想，有新的重要发现后要更新这些文件。

# 接下来：
* 根据你认知的Forgelight引擎先确定正常僵尸动画应该怎么用，来调查客户端实际从mrn以及僵尸的各种脚本相关的加载以及使用，再到驱动僵尸，再到接收到数据包的处理那部分的全链路是怎么样的导向，每一步中都需要什么样的使能条件，确认每一个函数或包的顺序、真实签名、数据类型、数据结构、默认值、回调、线程、消息循环等的真实内容。
* 按照Forgelight引擎的设计方针去尝试下钻打通看网络包如何驱动了mrn；同时也要按照mrn作为起点向上看，看到是什么网络包触发的。所以这是一个双向奔赴的确认过程，知道了链路就自然能推断怎么走什么要求,总结出要“让僵尸被指使追人并攻击”的完整过程和包信息以及下发到的哪些路由和函数最终到达mrn的驱动让客户端看到追击效果。
* 使用静态分析和动态调试cli进行逆向
* 按照完整正确的僵尸动画驱动链路更新服务端代码